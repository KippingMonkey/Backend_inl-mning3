@page
@model Musik_Affär.Pages.Reviews.IndexModel


@{
    ViewData["Title"] = "Index";
}

<h1>Mina Recensioner</h1>

<table class="table" style="width:100%">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Reviews[0].Product.Name)</th>
            <th> @Html.DisplayNameFor(model => model.Reviews[0].Product.Category)</th>
            <th> @Html.DisplayNameFor(model => model.Reviews[0].Product.Color)</th>
            <th> @Html.DisplayNameFor(model => model.Reviews[0].Product.Brand)</th>
            <th>Mitt betyg</th>
            <th>Ändra betyg</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Reviews) {
        <tr>
            <td> @Html.DisplayFor(modelItem => item.Product.Name)</td>
            <td> @Html.DisplayFor(modelItem => item.Product.Category)</td>
            <td> @Html.DisplayFor(modelItem => item.Product.Color)</td>
            <td> @Html.DisplayFor(modelItem => item.Product.Brand)</td>
            <td> @Html.DisplayFor(modelItem => item.Grade)</td>
            <td>
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="val-text"></div>
                    <input asp-for="@Model.Grade" class="form-control" type="number" max="5" min="1" value="@item.Grade"/>
                    <button asp-page-handler="Change" asp-route-id="@item.ID" type="submit">✅</button>
                    <button asp-page-handler="Delete" asp-route-id="@item.ID" type="submit">🗑️</button>
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
